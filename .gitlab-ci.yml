default:
  tags:
    - build
    - deploy

stages:
  - deploy
  - build

build:
  stage: build
  script:
    - docker build -t bot-app:1 .

deploy:
  stage: deploy
  script: 
    - docker compose down
    - docker compose up